# Dependency versions are declared once, in pyproject.toml, to avoid drift.
# This file exists for `pip install -r requirements.txt` compatibility and
# installs the package (with its pinned deps) in editable mode.
-e .
